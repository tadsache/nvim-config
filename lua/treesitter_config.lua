require'nvim-treesitter.configs'.setup {
  ensure_installed = { "lua", "yaml", "python"}, -- Add other languages as needed
   highlight = {
    enable = true,
  },
view = {
    enable = true,
  },
  
}

